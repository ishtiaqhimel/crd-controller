---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.2
  creationTimestamp: null
  name: ishtiaqs.crd.com
spec:
  group: crd.com
  names:
    kind: Ishtiaq
    listKind: IshtiaqList
    plural: ishtiaqs
    singular: ishtiaq
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Ishtiaq is the Schema for the ishtiaq API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: IshtiaqSpec defines the desired state of IshtiaqCRD
            properties:
              container:
                properties:
                  image:
                    type: string
                  port:
                    format: int32
                    type: integer
                type: object
              name:
                type: string
              replicas:
                format: int32
                type: integer
            required:
            - container
            - replicas
            type: object
          status:
            description: IshtiaqStatus defines the observed state of IshtiaqCRD
            properties:
              availableReplicas:
                format: int32
                type: integer
            required:
            - availableReplicas
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
